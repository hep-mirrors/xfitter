if(NOT APFEL_FOUND)
  message(STATUS "Skipping optional reaction FONLL_DISCC because APFEL library was not found")
  return()
endif()
set(TARGET reactionNC_SIA)
add_library(${TARGET} MODULE ReactionNC_SIA.cc)
target_link_libraries(${TARGET} PRIVATE xfitter evolutionQCDNUM APFEL)
install(TARGETS ${TARGET} DESTINATION ${DEST_MODULE})
install(FILES NC_SIA.yaml DESTINATION ${DEST_YAML}/reactions)
