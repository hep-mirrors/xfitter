#if(NOT QCDNUM_FOUND)
#  message(STATUS "Skipping optional reaction FFABM_DISNC because QCDNUM library was not found")
#  return()
#endif()

set(TARGET reactionFFABM_DISNC_CC)
add_library(${TARGET} MODULE ReactionFFABM_DISNC_CC.cc)
target_link_libraries(${TARGET} PRIVATE xfitter reactionBaseFFABM reactionBaseDISCC)
install(TARGETS ${TARGET} DESTINATION ${DEST_MODULE})
install(FILES FFABM_DISNC_CC.yaml DESTINATION ${DEST_YAML}/reactions)
