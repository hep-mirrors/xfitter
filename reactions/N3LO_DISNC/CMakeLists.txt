if(NOT APFELxx_FOUND)
  message(STATUS "Skipping optional reaction N3LO_DISNC because APFEL library was not found")
  return()
endif()
if(NOT QCDNUM_FOUND)
  message(STATUS "Skipping optional reaction N3LO_DISNC because QCDNUM library was not found")
  return()
endif()

set(TARGET reactionN3LO_DISNC)
add_library(${TARGET} MODULE ReactionN3LO_DISNC.cc)
target_link_libraries(${TARGET} PRIVATE xfitter reactionBaseDISNC APFELxx evolutionQCDNUM evolutionAPFELxx)
target_include_directories(${TARGET} PUBLIC .)
install(TARGETS ${TARGET} DESTINATION ${DEST_MODULE})
install(FILES N3LO_DISNC.yaml DESTINATION ${DEST_YAML}/reactions)
